// WAS TRYING TO CREATE A WIDGET. REFER YOUTUBE

import 'package:flutter/material.dart';

class tempViewer extends StatelessWidget {
  final Color textColor;
  final String data;
  final int width;
  tempViewer(
      {super.key,
      required this.textColor,
      required this.data,
      required this.width});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
